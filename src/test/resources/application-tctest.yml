spring:
  datasource:
    url: jdbc:tc:mariadb:10.3
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.MariaDB103Dialect
    open-in-view: false
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: create
